import React from "react";

const Signup = React.createClass({
	render() {
		return (
				<div className="container">
					<h2>Sign up!</h2>
					<br />
					<p className="lead">This is where you can make a new local account!</p>
					<p>(When the functionality is added that is..)</p>
					</div>
		);
	}
});

export default Signup;
